var withButtonItemClass = document.getElementsByClassName("button");

    for (var i = 0; i < withButtonItemClass.length; i++) {
        var text = withButtonItemClass[i].innerText;
        console.log(text);
    };
